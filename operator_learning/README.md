# Tutorial: Operator learning

This tutorial contains one notebooks:
1. [Fourier neural operator for the 1D Burger's equation](.\notebooks\fourier_neural_operator.ipynb)

And one additional file for plotting (Used for the tutorial slides):
+ [Plotting FNO trainig histories for comparing enriched and default training](.\notebooks\plot_combined_history.py)

## Installation
If you are using `uv`, you can create a virtual environment with all dependencies and the `dynamic_modeling` package by running

```bash
uv sync
```

If you are using `pip`, you can install `dynamic_modeling` in your virtual environment by running

```bash
pip install -e .
```